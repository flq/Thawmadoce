<Window x:Class="Thawmadoce.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Icon="thawmadoce.ico">
    <Grid>
        <Grid.Resources>
            
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl x:Name="Editor" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
        <ContentControl x:Name="AppItems" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,2,100,0" />
        <ContentControl x:Name="AppDialogs"
            Grid.Row="1" 
            VerticalAlignment="Bottom" 
            HorizontalAlignment="Stretch" />
    </Grid>
</Window>
